@model IEnumerable<CineTrackBE.Models.Entities.Genre>

@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>

@if (User.IsInRole("Admin"))
{
	<a class="btn btn-outline-info" asp-action="Create">Create New</a>
}
<table class="table table-dark my-3">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Id)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Name)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Id)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Name)
				</td>
				<td>
					@if (User.IsInRole("Admin"))
					{
						<a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-info m-1">Edit</a>
					}
					<a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info m-1">Details</a>
					@if (User.IsInRole("Admin"))
					{
						<a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-info m-1">Delete</a>
					}
				</td>
			</tr>
		}
	</tbody>
</table>
